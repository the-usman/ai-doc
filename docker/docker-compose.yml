# AI-Doc — Compose file locations
#
# Local development (from repo root):
#   docker compose up --build
#   → uses ../docker-compose.yml
#
# Production / Dokploy:
#   docker/docker-compose.prod.yml
#   → Container port 3000 in Dokploy UI (no host bind on 3000)
