"""LangChain chat application package (Phase 2).

Exposes the chat chain, tools, structured-output schema, conversation memory,
and the FastAPI/LangServe routes that serve the chatbot at ``/api/chat``.
"""
