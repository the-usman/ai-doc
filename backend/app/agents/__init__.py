"""LangGraph multi-agent pipeline (Phase 3).

A supervisor routes a task to specialised worker agents (DataAgent, ReportAgent)
and decides when the run is complete. The compiled graph, its typed state, the
nodes, persistence, and HTTP routes live in this package.
"""
